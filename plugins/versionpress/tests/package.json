{
  "name": "versionpress-tests",
  "version": "1.0.0",
  "description": "VersionPress tests",
  "repository": {
    "type": "git",
    "url": "https://bitbucket.org/agilio/versionpress"
  },
  "scripts": {
    "test": "echo Choose one of the `test:<test-type>` scripts",
    "test:unit-tests": "docker-compose run unit-tests || true",
    "test:selenium-tests": "docker-compose run selenium-tests || true",
    "cleanup-tests": "docker-compose down -v"
  },
  "devDependencies": {
    "chalk": "^0.5.1",
    "gulp": "^3.8.10",
    "selenium-standalone": "^4.5.3",
    "tcp-port-used": "^0.1.2",
    "yargs": "^1.3.3"
  }
}
